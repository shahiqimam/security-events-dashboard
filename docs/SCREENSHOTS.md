# Screenshots

Capture these pages after the stack is running and demo events have been ingested:

1. `/login`
2. `/dashboard`
3. `/events`
4. `/events/:id` with Overview, Raw Event, Notes, and History visible
5. `/sources`
6. Swagger at `/api/docs`
